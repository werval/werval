//
// This file is to be applied to every org.qiweb.modules submodules.
//

apply from: rootProject.file( '../gradle/java.gradle' )

dependencies {
    // Adding dependencies here will add the dependencies to all submodules.
    testCompile libs.awaitility
    testCompile libs.rest_assured
}
