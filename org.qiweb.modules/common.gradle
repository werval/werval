//
// This file is to be applied to every org.qiweb.modules submodules.
//

apply from: "$rootProject.projectDir.absolutePath/../gradle/java.gradle"

dependencies {
    // Adding dependencies here will add the dependencies to each submodule.
}
