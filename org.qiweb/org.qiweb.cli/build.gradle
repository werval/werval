
dependencies {
    compile project( ':org.qiweb.commands' )
    compile libs.cli
}

apply plugin: 'application'

mainClassName = 'org.qiweb.cli.DamnSmallDevShell'
applicationName = 'qiweb'

distZip {
    classifier = 'dist'
}

artifacts {
    archives distZip
}

// Uncomment to publish to QiWeb Local Repository
// publishing { publications { cliDist( MavenPublication ) { artifact distZip } } }
// task finalizeCliInstall( dependsOn: "publishCliDistPublicationToQiWebLocalRepository" )
// install.finalizedBy finalizeCliInstall
