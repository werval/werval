//
// Application Config for PluginRoutesTest
//

include "application"

app {
    plugins = [
        {
            plugin = org.qiweb.runtime.plugins.WithRoutesPlugin
            routesPrefix = "pre/fix/"
        }
    ]
}
