
apply from: "$rootProject.projectDir.absolutePath/../java.gradle"
apply plugin: 'groovy'
apply plugin: 'sonar-runner'

dependencies {

    compile gradleApi()
    groovy localGroovy()

    compile "org.qiweb:org.qiweb.devshell:$version"
    compile libraries.gradle_tooling
    
}

sonarRunner {
    sonarProperties {
        // property "sonar.language", "grvy"
    }
}

